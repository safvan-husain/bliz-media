---
import { services } from "../data/services";
import HorizontalServices from "./HorizontalServices";
import ServicesList from "./ServicesList.astro";
---

<section id="services" class="relative bg-zinc-50">
  {/* Subtle Grain Texture */}
  <div
    class="absolute inset-0 pointer-events-none opacity-[0.03] z-50 bg-[url('https://grainy-gradients.vercel.app/noise.svg')]"
  >
  </div>

  <div
    class="max-w-[var(--max-width-container)] mx-auto px-6 relative z-10 pt-32 pb-12"
  >
    <div class="flex flex-col md:flex-row justify-between items-center gap-12">
      <div class="max-w-2xl text-center md:text-start">
        <span
          class="text-primary font-bold tracking-[0.2em] text-sm uppercase mb-4 block"
          >What we do</span
        >
        <h2
          class="text-2xl md:text-6xl font-black tracking-tight text-[#0c1b33] leading-[0.85]"
        >
          OUR SERVICES
        </h2>
      </div>
      <div class="max-w-sm pb-2">
        <p class="text-lg text-center md:text-end font-medium text-[#0c1b33]/60 leading-relaxed">
          We think out of the box when it comes to creative.
        </p>
      </div>
    </div>
  </div>

  <div class="md:hidden">
    <ServicesList services={services} />
  </div>

  <div class="hidden md:block">
    <HorizontalServices client:media="(min-width: 768px)" services={services} />
  </div>
</section>
